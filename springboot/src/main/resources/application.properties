

spring.datasource.url=jdbc:postgresql://localhost:5433/posbd
spring.datasource.username=admin
spring.datasource.password=admin
spring.datasource.driverClassName=org.postgresql.Driver

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true

server.servlet.context-path=/pos
spring.jpa.properties.hibernate.globally_quoted_identifiers=true


# n�mero m�ximo de milissegundos que um cliente aguardar� por uma conex�o.
# se n�o houver conex�o disponivel em 20 segundos, vai dar erro de timeout.
spring.datasource.hikari.connection-timeout= 20000
# n�mero minimo de conexoes inativas mantidas pelo hirakiCP em um conjunto de conex�es
# quantidade em memoria aguardando para ser usada
spring.datasource.hikari.minimum-idle= 10
# m�ximo do pool de conex�o, podemos colocar o limite que quisermos. 40 j� � muito
spring.datasource.hikari.maximum-pool-size= 40
# Tempo ocioso da conex�o antes de morrer, ou seja,
# se a conex�o parar de fazer requisi��o, ainda permanece viva por 10 segundos
spring.datasource.hikari.idle-timeout=10000 
# salvando dados no banco automaticamente tudo que rodar 
spring.datasource.hikari.auto-commit=true 
